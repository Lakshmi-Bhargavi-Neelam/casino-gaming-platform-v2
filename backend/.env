# Project Settings
PROJECT_NAME="Casino Multi-Tenant Platform"
API_V1_STR="/api/v1"

# Security
# Generate a real secret key for production using: openssl rand -hex 32
SECRET_KEY="your-super-secret-key-change-this-in-production"
ACCESS_TOKEN_EXPIRE_MINUTES=11520  # 8 days

# Database
# Format: postgresql://user:password@postgresserver/db
DATABASE_URL="postgresql://postgres:techv1%403@localhost/casino_db_final"

# CORS
# Comma-separated list of allowed origins
BACKEND_CORS_ORIGINS="http://localhost:3000,http://localhost:8000"
